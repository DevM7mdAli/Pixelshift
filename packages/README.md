# Publishable packages

- `core` contains the shared image conversion API.
- `web-core` contains the main Lit component and generated Tailwind stylesheet.
- `react`, `angular`, and `vue` are thin framework adapters around `web-core`.
