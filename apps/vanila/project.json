{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "vanila",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "vite build", "cwd": "apps/vanila" }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": { "command": "vite", "cwd": "apps/vanila" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "apps/vanila"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run", "cwd": "apps/vanila" }
    }
  }
}
